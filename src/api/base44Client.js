// MIGRATED TO OWN BACKEND - This file kept for backwards compatibility
// Import the new API client instead
import apiClient from './apiClient.js';

// Export with base44 name for backwards compatibility
export const base44 = apiClient;

// Also export as default
export default apiClient;